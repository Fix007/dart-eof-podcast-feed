name: eof_podcast_feed
version: 1.1.0
description: eof_podcast_feed is a package for Dart and Flutter that Read and Parse RSS Feed
homepage: https://github.com/luizeof/dart-eof-podcast-feed
environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  http: ^0.12.2
  xml: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.15.7
